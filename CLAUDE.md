@AGENTS.md

# SFA アプリ

中小企業向けのシンプルな営業支援ツール(Next.js 16 App Router + Supabase + Tailwind v4 + shadcn/ui)。

## 構成
- `src/app/(app)/*` 認証必須のページ。`src/app/(auth)/login` ログイン。`src/proxy.ts` でセッション更新とリダイレクト。
- `src/actions/*` Server Actions(DB 更新はここに集約)。
- `src/lib/mail/*` メールアカウント(`accounts.ts`、複数可。パスワードは `crypto.ts` で暗号化して `mail_accounts` に保存。Gmail 以外の IMAP/SMTP サーバーも可: 993/465 は SSL、143/587 は STARTTLS 必須、`login_user` でメールアドレスと違うログイン ID。種別の判定と入力例は `providers.ts`)、IMAP 取り込み(`sync.ts`、送信済みフォルダは SPECIAL-USE → よくある名前の順で探す)、SMTP 送信(`smtp.ts`)、メールからの情報抽出(`extract.ts`、ANTHROPIC_API_KEY があれば Claude を使用)、担当者・取引先の紐付け(`link.ts`。取引先は企業ドメインならドメイン、フリーメール・携帯・プロバイダー(`isFreeMail`、`.ne.jp` は一律共有扱い)なら本文の会社名で決める。会社名の照合は `src/lib/company-name.ts` の `companyNameKey`(法人格・全角半角・空白を除いたキー)。会社名が取れないと「氏名(個人)」の仮取引先になり、後のメールで会社名が取れたら `upgradePlaceholderCompany` が移して空の仮取引先を消す。自社サイトのフォーム通知は本文の問い合わせ者を使う。フォームのラベルはテナント設定 `app_settings` の `form_labels_*` / 通知システムの送信元は `form_senders` で追加、`form-profile.ts` で読み込んで sync/inquiries に渡す)。誤登録の修正は `src/actions/links.ts`(スレッドの担当者・取引先の付け替え `relinkThread`、担当者・取引先の統合 `mergeContacts` / `mergeCompanies`、取引先一覧のチェック選択からの複数統合 `mergeManyCompanies` → `merge-selected-dialog.tsx`)。担当者の所属変更はメール・問い合わせの取引先にも波及(`src/lib/relink.ts`)。
- メール転送による受信(0026): IMAP が使えないメールサービス(Microsoft 365 など)向け。`mail_accounts.receive_mode`=forward のアカウントは IMAP を使わず、アカウントごとの受け口アドレス(`inbound_token`、形は環境変数 `INBOUND_ADDRESS_FORMAT` の `{token}`。catch-all の `t-{token}@in.…` でもプラスアドレスでも可)への自動転送で受信し、送信は従来どおり SMTP。全テナント共通の受信用メールボックス(運営側が用意、`INBOUND_IMAP_HOST/PORT/USER/PASSWORD`。未設定なら設定画面の選択肢に出ず保存も拒否)を `src/lib/mail/inbound.ts` の `syncForwardAccounts` が `syncMail` から開き、ヘッダー全体からトークンを探して自テナント宛てだけ `ingestParsedMail` し、取り込めたら箱から消す(失敗は残して次回やり直し。他テナント宛てには触らない)。差出人が自社アドレスなら BCC の控えとして outbound、件名が Fwd:/転送: の手動転送は本文の From 行から相手を拾う。cron の `/api/mail/sync` が最後に `cleanupInboundMailbox`(宛先不明を既読にして走査から外す、30 日超を削除。ここだけ service role で inbound_token の一覧を読む)。画面は `mail-account-dialog.tsx`(保存後に「次に、メールの転送を設定してください」へ切り替え)と `forward-setup.tsx`(受け口アドレス・サービス別手順 `FORWARD_GUIDES`・`checkForwardArrival`・`regenerateInboundAddress`)。接続テスト・ヘルスチェック・添付の後追いは転送のアカウントで IMAP を飛ばす。
- Server Action のエラー: 利用者に見せるメッセージは `throw userError("〜してください")`(`src/lib/errors.ts`)で投げ、クライアントは `toast.error(actionErrorMessage(e))` で表示する。本番の Next.js は Error の message をブラウザに送らず(React #441)、digest だけ届くので、メッセージを digest に載せて運んでいる。`new Error` で投げると本番では「処理に失敗しました」としか出ない。
- エラー記録と通知: `src/lib/log.ts`(`logSystem` で `system_logs` に記録。error は設定画面の通知先メールと `ALERT_WEBHOOK_URL` へ通知、同じ source は 30 分に 1 回)。`src/instrumentation.ts` の `onRequestError` でサーバー側の未捕捉エラーを自動記録(Cookie から操作ユーザーとテナントを調べて通知の件名・本文に入れる。`src/lib/supabase/request-context.ts`、セッションのリフレッシュはしない)。通知先は 2 系統: テナントのエラーはそのテナントの設定画面「通知先メールアドレス」(`app_settings.alert_emails`)+ 環境変数 `ALERT_WEBHOOK_URL`。運営側は運営管理 /admin/settings の「エラー通知先」(`operator_settings` の `alert_emails` / `alert_lark_webhook` / `alert_lark_secret`、`src/lib/alerts.ts`)で、システム全体のエラー・各テナントのエラーの控え・新規申し込みを受ける(`getOperatorAlertTargets`)。Lark のカスタム Bot は `postWebhook` が URL で判定して {msg_type:"text"} + 署名で送る。`/api/health` は外形監視用(認証なしは DB のみ、cron/ログイン時は IMAP/SMTP も確認)。ログは `/settings/logs`。
- `supabase/migrations/*.sql` スキーマ。`node scripts/apply-migrations.mjs` で Management API 経由で適用。
- `src/lib/types.ts` DB 行の型とステージ定義。
- 担当者は2種類: `contacts`(取引先側の担当者、メールから自動登録)と `members`(自社の営業担当者、`deals.owner_id` の参照先)。ログインは招待制: 営業担当者ページの「招待」で `auth.admin.generateLink` のリンクを自アプリの SMTP で送り、`/auth/confirm` → `/set-password` でパスワードを設定する。自己登録は無効。
- UI の用語: 取引先=companies、担当者=contacts、営業担当者=members。
- タグ(0014): `tags`(テナントごと、設定画面で最大 20 個、既定 6 個はテナント作成時にトリガーで投入)、`email_tags` / `contact_tags`。メール一覧の選択からタグ付けするとスレッド内の全メールと紐付く担当者にも付く(`src/actions/tags.ts`)。担当者一覧はタグで絞り込み、`/api/contacts/export` で CSV。埋め込みは `tags:email_tags(tag:tags(...))`、絞り込みは `filter_tags:email_tags!inner(tag_id)` + `.eq("filter_tags.tag_id", id)`。
- メールの自動タグ付け(0022): `email_tag_rules`(判定場所 `field`=subject/from/to(to は To+CC のアドレス、0027)、`keywords` は「、」区切りでいずれか、`tag_id`、最大 20 個)。照合は `src/lib/tag-rules.ts`(NFKC + 小文字化 + 空白除去で比較)。同期の `ingestParsedMail` が挿入直後に `matchTagRules` → `tagThreads` でスレッド内のメールにタグを付ける(担当者には付けない)。設定画面のタグの下 `tag-rule-settings.tsx`(`src/actions/tag-rules.ts`: 追加・編集・削除・「過去分にも適用」)。タグ名が「削除リスト」(`DELETE_LIST_TAG_NAME`)のメールは `createInquiriesFromEmails` が問い合わせにしない。
- マルチテナント(0009 以降): 契約企業は `tenants`、所属は `profiles.tenant_id`。業務テーブルは全部 `tenant_id` 列 + RLS(`tenant_isolation`)で分離し、insert 時は DB トリガーが `current_tenant_id()` で補う。
  - ログインユーザーのクライアント(`createClient`)なら既存コードのまま自テナントに絞られる。cron などユーザーが居ない処理は `src/lib/supabase/tenant.ts` の `createTenantClient(tenantId)`(要 `SUPABASE_JWT_SECRET`)でテナントごとに回す。
  - `createAdminClient`(service role)は RLS を通らないので業務テーブルの読み書きに使わない(使うのは auth.admin、Storage の実体操作、tenants 一覧、tenant_id が null のシステムログのみ)。service role で tenant_id 無しに insert すると例外になる。
  - 新しいテーブルを足すときは `tenant_id uuid not null references tenants(id) on delete cascade` + `set_tenant_id` トリガー + `tenant_isolation` ポリシーを必ず付ける。一意制約は `(tenant_id, ...)` の複合にする。
  - Storage(email-attachments)のパスは `<tenant_id>/...`。テナント作成は運営管理画面 `/admin/tenants/new`(または `node scripts/create-tenant.mjs`)。どちらも DB 関数 `create_tenant`。
- 運営管理(0010 以降): `/admin`(`src/app/(admin)`)は `operators` に登録された auth ユーザーのみ(`is_operator()`)。運営者は `operators.is_super` のスーパーユーザー(akfujii@art-trading.co.jp)が /admin/users からメールで招待する。未登録のメールなら運営専用アカウント(user_metadata operator=true → `handle_new_user` がテナントに所属させない。profiles.tenant_id は null)で、テナント側の画面にはログインできず、削除で auth ユーザーごと消える。運営側テナント(created_at が最古の自社)の利用者は既存のログインのまま運営者にもなれる(テナント側と /admin の両方に入れる。magiclink で /admin へ案内、削除は operators から外すだけ)。他社テナントの利用者は運営者にできない。
- 招待メールの文面は `src/lib/mail/templates.ts`(既定文面 + `mail_templates` テーブルの上書き、`{{name}}` などの差し込み)。運営管理 /admin/mail-templates で編集。新しい送信メールを足すときは同じ仕組みを使う。`src/actions/admin.ts` が service role でテナント横断に tenants / operator_settings / members(招待)を扱う。料金・既定上限は `operator_settings`、月額計算は `src/lib/pricing.ts`。
  - 上限(ユーザー数=profiles+招待中、メールアカウント数、容量=添付+本文バイト)と利用可否(停止・解約・お試し期限切れは閲覧のみ)は `src/lib/tenant-quota.ts`。書き込み系 Server Action の入口で `assertTenantWritable` / `assertCanAddUser` / `assertCanAddMailAccount` / `assertStorageAvailable` を呼ぶ。
- 申し込みと課金(0015): Web 申し込みは `/signup`(`src/actions/signup.ts`)。フォーム → `signup_requests` に保存して確認メール(テンプレート `signup_confirm`、運営側テナントのメールアカウントから送信)→ `/signup/verify?token=` の「アカウントを開設する」ボタンで `create_tenant(source=signup)` → Supabase の招待リンクへリダイレクトしてパスワード設定。カード登録は不要(お試し期間はそのまま)。
  - Stripe は `src/lib/stripe.ts` に集約。商品は初回に自動作成して ID を `operator_settings`(`stripe_product_*`)に控え、価格は運営の料金設定(単価)に一致する Price を探して無ければ作る。請求は数量課金(基本 1 + ユーザー追加 n + メールアカウント追加 n)に消費税 10% の Tax Rate(`ensureTaxRate`、`operator_settings.stripe_tax_rate_10`)を外税で付ける。画面の税込表示は `pricing.ts` の `withTax`。招待・削除・メールアカウント追加/削除の Server Action で `after(() => syncTenantBilling(tenantId))` を呼んで数量を同期する(利用数に影響する処理を足したら同じように呼ぶ)。
  - テナント側は `/settings/billing`(`src/actions/billing.ts`): Checkout(お試し中なら `trial_end` を付けて課金開始をお試し終了日にする)、カスタマーポータル(支払い方法・請求書・解約)。契約状態は Webhook `/api/stripe/webhook`(`stripe_events` で重複排除)が `tenants` に写す(`applySubscriptionToTenant`: trialing→trial、active→active、past_due→active+past_due、unpaid/paused→suspended、canceled→cancelled)。運営が手で停止(suspended)したテナントは Webhook で上書きしない。
  - 環境変数 `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET`。未設定なら課金画面は「準備中」表示で申し込みだけ動く。`NEXT_PUBLIC_SITE_URL` があれば確認メール・Checkout の戻り先に使う。

- 案件の行動(0019・0020): `deal_activities`(種類 `kind_id` → `activity_kinds`、本文、期限 due_at、完了 done_at)。種類はテナントごとに `activity_kinds`(名前・アイコンキー `icon`・sort_order、最大 20 個、既定 6 個はテナント作成時にトリガーで投入)で、設定画面の「行動の種類」から追加・編集・削除(使用中と最後の 1 個は不可)・上下の並び替え(`src/actions/activity-kinds.ts`、アイコン一覧は `src/lib/activity-kinds.ts`)。タグも同じ `reorder-buttons.tsx` で並び替え(`reorderTags`)。行動の埋め込みは `kind:activity_kinds(id,name,icon)`。`src/actions/activities.ts` で登録・編集・完了・削除、期限超過の判定は `src/lib/activities.ts` の `dueState`(今日の判定は日本時間)。案件詳細の「行動」タブ、カンバンのカード(期限超過・今日の件数)、ダッシュボードの「今日やること」、一覧ページ `/activities`(期限の状態ごとにグループ)、サイドバー「行動」の期限超過バッジに表示。共通の 1 行は `activity-row.tsx`(完了チェック付き)。

- メールのゴミ箱(0028): 削除は物理削除ではなく `emails.deleted_at/deleted_by`(スレッド単位)。`emails` の RLS は select だけ `deleted_at is null` に絞るので、emails を読む既存コードは触らずにゴミ箱の中身が隠れる(埋め込み・未読数・同期の重複判定も含む。message_id の一意制約は残るので再取り込みされない)。update/delete の where と PostgREST の RETURNING にも select ポリシーが掛かるため、移動・復元・完全削除は security definer の DB 関数 `email_trash_move` / `email_trash_restore`(thread_key 配列)/ `email_trash_purge`(id 配列)で行い、ゴミ箱画面 `/inbox/trash`(`trash-list.tsx`)だけが security_barrier ビュー `email_trash`(自テナントの削除済み + `deleted_by_name`)を読む。`src/lib/trash.ts`(`TRASH_RETENTION_DAYS`=14 と `trashDaysLeft`。クライアント部品からも読むのでサーバー専用のものを import しない)、`src/lib/mail/trash.ts`(`purgeEmails` は添付の実体 → 行の順、`purgeExpiredEmailTrash` は cron `/api/mail/sync` が全テナントで実行)。Server Action は `deleteEmailThreads`(threadKeys を返し、トーストの「元に戻す」→ `restoreEmailThreads`)/ `purgeEmailThreads`。ゴミ箱の中身も容量に含まれる。メール一覧は `?account=`(`account-filter.tsx`、アカウントが 2 つ以上のときだけ表示。絞り込んでいないときは行にアカウント名)と 300 通ずつ `?n=` で「さらに表示」(PostgREST の 1000 行上限があるので 1000 行ずつ range で読む)。
- 問い合わせの担当者・メモ・タグ(0024): `inquiries.owner_id`(members、`updateInquiryOwner`。付けたとき status が new なら in_progress に進める)、`memo` + `memo_updated_at/by`(単一欄、`updateInquiryMemo`)。タグは問い合わせ専用のテーブルを持たず、紐付くメール(`emails.inquiry_id`)の `email_tags` をそのまま表示・編集(`inquiry-tags.tsx` → `setEmailTags`)。一覧の絞り込みは `?owner=none|<member_id>` と `?tag=`(タグは email_tags → emails.inquiry_id で問い合わせ id を集めて `.in`)。案件化の `NewDealDialog` に `owner_id` と `memo` を引き継ぐ。
- アポイント Todo(0023): 案件の作成・更新でアポイント日時があれば `src/lib/appointment-todo.ts` の `syncAppointmentTodo` が行動の種類「アポイント」(既定で投入、無ければ作る)の `deal_activities` を期限 = アポ日時で自動登録し、日時変更で未完了 Todo の期限を追従させる。
- カンバンのカード(`kanban-board.tsx`): アポ日時はステージに関係なく `AppointmentLine` で表示(`src/lib/activities.ts` の `appointmentState`、過ぎたら「n 日経過」、`APPOINTMENT_STALE_DAYS`=14 日超で赤)。未完了 Todo は `deals/page.tsx` が `open_todos`(期限順、種類名付き)を載せ、`TodoLine` で表示。件数は右上の「カードの Todo」(3 / 1 / 件数だけ)で切り替え、`src/lib/local-pref.ts` の `useLocalPref` で localStorage に記憶(サーバー描画は既定値)。
- Google カレンダーに追加: 期限のある未完了の行動に `calendar-add-button.tsx`(`src/lib/calendar-link.ts` の `googleCalendarUrl`、Google の予定作成 URL を新しいタブで開くだけ。認証・API・同期は無し。23:59 保存の「終日」は終日予定、時刻ありは 1 時間)。`activity-row.tsx`(ダッシュボード・行動一覧)と `deals/activities.tsx`(案件詳細、`deal` prop で案件名・取引先を渡す)に表示。ICS 購読や OAuth 連携は要望が出てから。
- メールテンプレート(0025): `email_templates`(テナントごと。`member_id` が null なら会社共通、入っていれば自分専用。各 10 個まで、`src/actions/email-templates.ts`)。設定画面の `email-template-settings.tsx` で登録し、`(app)/layout.tsx` が会社共通 + 自分専用を `SignatureProvider`(`useMailDefaults`)で返信フォーム・新規作成ダイアログに配る。差し込み項目は `src/lib/mail/merge.ts`(`{{取引先}}` など日本語キー、`renderMerge` / `unresolvedMerges`)。置き換えはテンプレートを選んだ時点に画面側で行い(`template-select.tsx` の `applyTemplate`)、値はページから `merge` prop で渡す(スレッド返信・案件・取引先・担当者一覧。受信トレイの新規作成は宛先の blur で `lookupContactByEmail`)。送信前に `send-preview-dialog.tsx` で確認し、`{{…}}` が残っていれば送れない(`sendEmail` も拒否)。返信の引用は本文に含まれていなければ末尾に付ける。
- 日付入力: 行動の期限・案件のアポイント日時・受注予定日は `src/components/date-picker/date-picker.tsx`(日付ボタン + 「日付を選ぶ」で 2 週間タイル + 時刻ボタン。hidden input で datetime-local と同じ形式を渡すのでサーバーの `parseLocalInput` はそのまま)。「時刻なし(終日)」は 23:59 で保存し `fmtDue` で日付だけ表示。祝日は `src/lib/holidays.ts`(固定表、2028 年まで。毎年足す)。新しく日時を入力する画面を作るときは `<input type="datetime-local">` ではなくこの部品を使う。

- 文書は利用者向けと運営者向けで分ける(利用者に不要な運営の情報は利用者マニュアルに書かない)。どちらも本文は Claude の Artifact と同じ「本文だけ」の HTML で、`node scripts/build-docs.mjs`(`npm run build` でも自動実行)が noindex 付きの完全な HTML にする。
  - 利用者向け(公開): `docs/*.html`(`manual.html` 利用者マニュアル、`flow.html` 業務フロー図。マニュアル冒頭にも同じ SVG を埋め込み)→ `public/docs/` に書き出し、`next.config.ts` の rewrites で `/docs`, `/docs/manual`, `/docs/flow` として配る(X-Robots-Tag も付与、proxy で公開パス)。テナント側のサイドバーと設定画面からリンク。
  - 運営者向け(非公開): `docs/admin/*.html`(`manual.html` 運営マニュアル=運営管理画面・料金・通知・転送の受け口の構築と運用・環境変数・定期処理、`test-spec.html` テスト仕様書)→ public には置かず `src/content/admin-docs.generated.ts` に埋め込み、`src/app/(admin)/admin/docs/[slug]/route.ts` が `is_operator()` を確認して `/admin/docs/manual`, `/admin/docs/test-spec` で配る(運営者以外は 404)。運営管理のメニュー「運営マニュアル」からリンク。旧 `/docs/test-spec` は redirect。
  - 文書を直すときは `docs/` を編集して build-docs を実行し、`public/docs/` と `admin-docs.generated.ts` もコミットする。秘密の値(パスワード類)は文書に書かない。

- LP(紹介ページ): 専用ドメイン `lp.sfa.art-trading.net`(環境変数 `NEXT_PUBLIC_LP_HOST`。Vercel の同じプロジェクトにドメイン追加、DNS は CNAME → cname.vercel-dns.com)。`src/lib/supabase/proxy.ts` がそのホストでは `/` を `/lp` に rewrite し、他のパス(/signup, /login など)は `NEXT_PUBLIC_SITE_URL` の本体へ 307。本体で未ログインの `/` は LP ドメインへ 307(`NEXT_PUBLIC_LP_HOST` 未設定なら従来どおり `/lp` に rewrite)。文言・構成・開発者写真は `src/content/lp.ts`、見た目は `src/app/lp/lp.css`(`.lp` にスコープ)、料金は operator_settings から自動。写真は public/lp/ に置いて lp.ts の verdict.founder を設定。

## ドメインの流れ
受信メール(顧客/担当者は自動登録) → メール一覧で選択して問い合わせ(inquiries)に登録 → アポ取得で案件化(deals, stage=appointment) → カンバンでステージ管理 → 成約時に月次売上(revenues)を必須入力。

## コマンド
- `npm run dev` / `npm run build` / `npx eslint src` / `npx tsc --noEmit`
